Reqd: testFile.jpg in the same folder
Outputs: outputFile.jpg
To Run, do sudo mn and then in each of the xterm,

h2: run bash limit_h2.sh before calling python3 receiver.py
h1: run bash limit_h1.sh before calling python3 sender.py

The .sh files basically runs the command to simulate prop delay, packet loss, and the bandwidth limit

